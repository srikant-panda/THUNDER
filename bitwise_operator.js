/**
 * Bitwise operator
 * 
 * 1. Bitwise AND = `&`  => give 1 if both bits are 1 otherwise 0
 * 2. Bitwise OR = `|`  => give 1 if either of the bit is 1
 * 3. Bitwise XOR = `^`  => give 1 if only one of the bit is 1  
 * 4. Bitwise NOT = `~`  => give 1 if the bit is 0 otherwise 0
 * 5. Left shift = `<<`  => shift the bits to the left
 * 6. Right shift = `>>`  => shift the bits to the right
 * 7. Zero fill right shift = `>>>`  => shift the bits to the right with zero fill
 */


// Bitwise AND
// const a = 10;
// const b = 22;

// console.log(a&b);

//Bitwise left shift

// const a = 10;
// const b = 2;
// console.log(a<<b)
